Scratch Adventure Code Realm — Connected Build

Login → Google Apps Script → Google Sheets → Dashboard → Map → Missions → Final Quest → Hall of Legends.
Google Sheets remains the source of truth. No schema changes.
API URL is embedded in js/api.js and login.html.
Mission scores are saved with action saveMission.
Protected pages require sessionStorage codeRealmStudent.
